import Section from '../Section/Section';
import SectionTitle from '../Section/SectionTitle/SectionTitle';

function Partners() {
  return (
    <Section>
      <SectionTitle title="Наші партнери"></SectionTitle>
    </Section>
  );
}

export default Partners;
